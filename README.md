# Contao Multi-Step Registration

Reusable Contao 5.7 content element for member registration split into configurable steps.

The content element type is `huh_multi_step_registration` and is registered in the `member` category.
